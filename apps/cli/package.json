{
  "name": "@harness/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "harness": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && chmod +x dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@harness/core": "workspace:*",
    "@harness/dashboard": "workspace:*",
    "@harness/shared": "workspace:*",
    "commander": "^12.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^3.2.7"
  }
}
